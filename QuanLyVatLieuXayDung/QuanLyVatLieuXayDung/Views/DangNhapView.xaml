<Window x:Class="QuanLyVatLieuXayDung.Views.DangNhapView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyVatLieuXayDung.ViewModels"
        mc:Ignorable="d"
        Title="DangNhapView" Height="450" Width="800"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        d:DesignHeight="550" d:DesignWidth="800">
    <Window.DataContext>
        <local:DangNhapViewModel/>
    </Window.DataContext>
        <Grid Background="#ECEFF1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*" />
                <ColumnDefinition Width="6*" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="#263238" CornerRadius="0,30,30,0">
                <Grid>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="30">
                        <TextBlock Text="WELCOME TO!" Foreground="White" FontSize="36" FontWeight="Bold" FontStyle="Oblique" TextAlignment="Center"/>
                        <TextBlock Height="30"/>
                        <TextBlock Text="Xây Dựng 3V" Foreground="White" FontSize="24" FontWeight="Bold" TextAlignment="Center"/>
                        <TextBlock Text="Hệ thống quản lý vật liệu hiện đại" Foreground="White" Opacity="0.8" TextAlignment="Center" Margin="0,10,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="{DynamicResource MaterialDesignPaper}" CornerRadius="0,15,15,0">
                <Grid Margin="50,0">

                    <StackPanel x:Name="LoginForm" VerticalAlignment="Center" MaxWidth="350" HorizontalAlignment="Stretch">
                        <TextBlock Text="Đăng nhập" FontSize="28" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBlock Text="Vui lòng nhập tài khoản của bạn để tiếp tục" Opacity="0.6" Margin="0,0,0,30"/>

                        <TextBox materialDesign:HintAssist.Hint="Tên đăng nhập" 
                             materialDesign:TextFieldAssist.HasLeadingIcon="True"
                             materialDesign:TextFieldAssist.LeadingIcon="Account"
                                 Text="{Binding UserName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,20" FontSize="15" Height="50" VerticalContentAlignment="Center"/>

                        <PasswordBox materialDesign:HintAssist.Hint="Mật khẩu" 
                                 materialDesign:TextFieldAssist.HasLeadingIcon="True"
                                 materialDesign:TextFieldAssist.LeadingIcon="Lock"
                                 x:Name="txtPassword"
                                 PasswordChanged="TxtPassword_PasswordChanged"
                                 Margin="0,0,0,30" FontSize="15" Height="50" VerticalContentAlignment="Center"/>

                        <Button Content="ĐĂNG NHẬP" Height="45" 
                            Command="{Binding LoginCommand}"
                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                            Background="#FF8F00" Foreground="White" BorderBrush="#FF8F00"
                            FontWeight="Bold" FontSize="15"/>
                    </StackPanel>
                </Grid>
            </Border>

        </Grid>
</Window>
