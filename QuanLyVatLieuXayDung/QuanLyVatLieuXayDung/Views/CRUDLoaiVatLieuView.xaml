<Window x:Class="QuanLyVatLieuXayDung.Views.CRUDLoaiVatLieuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyVatLieuXayDung.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:uc="clr-namespace:QuanLyVatLieuXayDung.UserControlVLXD"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        ResizeMode="NoResize"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Name="window"
        mc:Ignorable="d"
        Title="Loại vật liệu" Height="500" Width="600">
    <Window.DataContext>
        <local:LoaiVatLieuViewModel/>
    </Window.DataContext>
    <Grid Background="#ECEFF1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <uc:ucControlBar Tag="{Binding Title,ElementName=window}" ></uc:ucControlBar>
        </Grid>

        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <materialDesign:Card Grid.Row="0" Margin="0,0,0,10">
                <StackPanel Margin="10">
                    <Label Content="Tên loại vật liệu: " Margin="0,0,0,5" FontWeight="SemiBold"></Label>
                    <Border CornerRadius="5" BorderBrush="#B0BEC5" BorderThickness="1" Height="35" Background="White">
                        <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=LostFocus, Mode=TwoWay}" 
                                 Background="Transparent" 
                                 BorderThickness="0" 
                                 VerticalContentAlignment="Center" 
                                 Padding="10,0"/>
                    </Border>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Row="1" Margin="0,0,0,10" VerticalAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
                        <Border CornerRadius="5" BorderBrush="#B0BEC5" BorderThickness="1" Background="White" Height="35">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Magnify" Width="20" Height="20" VerticalAlignment="Center" Foreground="#90A4AE" Margin="10,0,5,0"/>
                                <Grid>
                                    <TextBlock Text="Tìm kiếm theo tên loại vật liệu..." Foreground="#B0BEC5" VerticalAlignment="Center" Margin="5,0,0,0" FontSize="13" IsHitTestVisible="False">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Text, ElementName=txtSearch}" Value="">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Text, ElementName=txtSearch}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBox x:Name="txtSearch" Width="300" 
                                             BorderThickness="0" Background="Transparent" VerticalContentAlignment="Center"
                                             Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" FontSize="13" Padding="5,0,5,0"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                <ListView Grid.Row="1" ItemsSource="{Binding LoaiVatLieuView}" SelectedItem="{Binding SelectedLoai, Mode=TwoWay}" BorderThickness="0">
                    <ListView.Resources>
                        <Style TargetType="{x:Type GridViewColumnHeader}">
                            <Setter Property="Padding" Value="10,8" />
                            <Setter Property="BorderBrush" Value="#BDC3C7" />
                            <Setter Property="Background" Value="#FAFAFA" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                                        <Border BorderThickness="0,0,1,1" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>

                        <Style TargetType="{x:Type ListViewItem}">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Height" Value="35"/>
                        </Style>
                    </ListView.Resources>

                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="STT" Width="55" DisplayMemberBinding="{Binding STT}"></GridViewColumn>
                            <GridViewColumn Header="Mã loại" Width="100" DisplayMemberBinding="{Binding ID}"></GridViewColumn>
                            <GridViewColumn Header="Tên loại vật liệu" Width="400" DisplayMemberBinding="{Binding DisplayName}"></GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
                </Grid>
            </materialDesign:Card>

            <materialDesign:Card Grid.Row="2">
                <WrapPanel HorizontalAlignment="Center" Margin="0,5,0,5">
                    <WrapPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Height" Value="35"></Setter>
                            <Setter Property="Width" Value="100"></Setter>
                            <Setter Property="Margin" Value="10,5,10,5"></Setter>
                            <Setter Property="Background" Value="#4CAF50"></Setter>
                            <Setter Property="Foreground" Value="White"></Setter>
                            <Setter Property="Cursor" Value="Hand"></Setter>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#388E3C"></Setter>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </WrapPanel.Resources>

                    <Button Command="{Binding AddCommand}" Content="Thêm"></Button>
                    <Button Command="{Binding UpdateCommand}" Content="Sửa" Background="#FF9800"></Button>
                    <Button Command="{Binding RemoveCommand}" Content="Xóa" Background="#F44336"></Button>
                </WrapPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</Window>