<Window x:Class="QuanLyVatLieuXayDung.Views.ThongTinNguoiDungView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:uc="clr-namespace:QuanLyVatLieuXayDung.UserControlVLXD"
        xmlns:local="clr-namespace:QuanLyVatLieuXayDung.ViewModels"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        ResizeMode="NoResize"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Name="wdThongTinUser"
        Title="Thông tin cá nhân người dùng" Height="auto" SizeToContent="Height" Width="550">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </Window.Resources>
    
    <Grid Background="#ECEFF1">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <uc:ucControlBar Tag="{Binding Title, ElementName=wdThongTinUser}"></uc:ucControlBar>
        </Grid>

        <Grid Grid.Row="1" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <materialDesign:Card Grid.Row="0" Margin="0,0,0,10" Padding="10">
                <GroupBox Header=" Chi tiết hồ sơ cá nhân " >
                    <StackPanel Margin="0,10,0,0">

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="Mã nhân viên:" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding ID}" IsReadOnly="True" Background="#F5F5F5" BorderThickness="1" BorderBrush="#B0BEC5" Height="30" VerticalContentAlignment="Center" Padding="5,0"/>
                        </Grid>

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="Tên đăng nhập:" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding UserName}" IsReadOnly="True" Background="#F5F5F5" BorderThickness="1" BorderBrush="#B0BEC5" Height="30" VerticalContentAlignment="Center" Padding="5,0"/>
                        </Grid>

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="Tên hiển thị (*):" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderThickness="1" BorderBrush="#B0BEC5" Height="30" VerticalContentAlignment="Center" Padding="5,0"/>
                        </Grid>

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="Quyền tài khoản:" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding RoleName}" IsReadOnly="True" Background="#F5F5F5" BorderThickness="1" BorderBrush="#B0BEC5" Height="30" VerticalContentAlignment="Center" Padding="5,0"/>
                        </Grid>

                        <Button Command="{Binding TogglePasswordPanelCommand}" Content="Đổi mật khẩu tài khoản ?" HorizontalAlignment="Right" Margin="0,10,0,0" Background="Transparent" BorderThickness="0" Foreground="#2980B9" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline" Foreground="#2C3E50"/>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </GroupBox>
            </materialDesign:Card>

            <materialDesign:Card Grid.Row="1" Margin="0,0,0,10" Padding="10" 
                                 Visibility="{Binding IsChangePasswordVisible, Converter={StaticResource BoolToVisConverter}}">
                <GroupBox Header=" Thiết lập mật khẩu mới " BorderBrush="#E74C3C">
                    <StackPanel Margin="0,10,0,0">

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="Mật khẩu hiện tại:" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding CurrentPasswordInput, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderThickness="1" BorderBrush="#B0BEC5" Height="30" VerticalContentAlignment="Center" Padding="5,0"/>
                        </Grid>

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="Mật khẩu mới:" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderThickness="1" BorderBrush="#B0BEC5" Height="30" VerticalContentAlignment="Center" Padding="5,0"/>
                        </Grid>

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="Xác nhận mật khẩu:" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderThickness="1" BorderBrush="#B0BEC5" Height="30" VerticalContentAlignment="Center" Padding="5,0"/>
                        </Grid>

                    </StackPanel>
                </GroupBox>
            </materialDesign:Card>

            <materialDesign:Card Grid.Row="2" Padding="5">
                <WrapPanel HorizontalAlignment="Center">
                    <WrapPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Height" Value="35"/>
                            <Setter Property="Width" Value="120"/>
                            <Setter Property="Margin" Value="10,5"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </WrapPanel.Resources>

                    <Button Command="{Binding UpdateInfoCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Content="Lưu cập nhật" Background="#43A047"/>
                    <Button Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Content="Hủy bỏ" Background="#546E7A"/>
                </WrapPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</Window>