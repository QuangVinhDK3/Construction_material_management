<UserControl x:Class="QuanLyVatLieuXayDung.Views.TrangChuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyVatLieuXayDung.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:uc="clr-namespace:QuanLyVatLieuXayDung.UserControlVLXD"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1000">
    <UserControl.DataContext>
        <local:TrangChuViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Background" Value="#2C3E50"/>
            <Setter Property="Padding" Value="15"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#1B3A4B">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="130*"/>
            <RowDefinition Height="250*"/>
            <RowDefinition Height="240*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50" Padding="15,10" Margin="0,0,0,15">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Tổng quan hệ thống" Foreground="White" FontWeight="Bold" FontSize="20" Margin="0,0,0,2"/>
                <TextBlock Text="Xem nhanh thống kê và dữ liệu thống kê" Foreground="#BDC3C7" FontSize="12"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="15,0,15,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                <Grid>
                    <Image Source="/Assets/Icon/staff.png" Stretch="Uniform" Width="24" Height="24" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <TextBlock Foreground="White" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Bottom" Text="Nhân viên"/>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
                <Grid>
                    <Image Source="/Assets/Icon/service.png" Stretch="Uniform" Width="24" Height="24" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <TextBlock Foreground="White" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Bottom" Text="Khách hàng"/>
                </Grid>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource StatCardStyle}">
                <Grid>
                    <Image Source="/Assets/Icon/material.png" Stretch="Uniform" Width="24" Height="24" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <TextBlock Foreground="White" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Bottom" Text="Loại vật liệu"/>
                </Grid>
            </Border>

            <Border Grid.Column="6" Style="{StaticResource StatCardStyle}">
                <Grid>
                    <Image Source="/Assets/Icon/bill.png" Stretch="Uniform" Width="24" Height="24" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <TextBlock Foreground="White" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Bottom" Text="Hóa đơn"/>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Margin="15,0,15,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6.5*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="3.5*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="15"  Background="#2C3E50" Padding="10">
                <lvc:CartesianChart Margin="0 30 30 0" Series="{Binding DoanhThu}" Foreground="{DynamicResource MainTextColor}">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Tháng" Labels="{Binding Labels}" Foreground="{DynamicResource MainTextColor}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Số tiền" LabelFormatter="{Binding Formatter}" Foreground="{DynamicResource MainTextColor}"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Border>

            <Border Grid.Column="2" CornerRadius="15" Background="#2C3E50" Padding="10">
                <lvc:PieChart Series="{Binding ChucVu}" InnerRadius="30" LegendLocation="Bottom" Foreground="{DynamicResource MainTextColor}"/>
            </Border>
        </Grid>

        <Border Grid.Row="3" Margin="15,0,15,15" Background="#2C3E50" CornerRadius="15" Padding="5">
            <materialDesign:Card UniformCornerRadius="12" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                <ListView Background="#2C3E50" ItemsSource="{Binding DSTonKho}" SelectedItem="{Binding SelectedType, Mode=OneWayToSource}">
                    <ListView.Resources>
                        <Style TargetType="{x:Type ListViewItem}">
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Height" Value="35"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#34495E"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#1ABC9C"/>
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>

                        <Style TargetType="{x:Type GridViewColumnHeader}">
                            <Setter Property="Background" Value="#2C3E50" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="FontWeight" Value="Bold" />
                            <Setter Property="Padding" Value="10,5" />
                            <Setter Property="BorderBrush" Value="#34495E" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                                        <Border BorderThickness="0,0,1,1" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.Resources>

                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="STT" Width="50">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding STT}" TextAlignment="Center" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Header="Tên vật liệu" Width="350">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding VatLieu.DisplayName}" TextAlignment="Center" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Header="Số lượng tồn" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Count}" TextAlignment="Center" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Header="Đơn vị tính" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding LoaiVatLieu.DisplayName}" TextAlignment="Center" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </materialDesign:Card>
        </Border>
    </Grid>
</UserControl>