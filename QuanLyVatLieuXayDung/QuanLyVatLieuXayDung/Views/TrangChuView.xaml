<UserControl x:Class="QuanLyVatLieuXayDung.Views.TrangChuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyVatLieuXayDung.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:uc="clr-namespace:QuanLyVatLieuXayDung.UserControlVLXD"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1000">
    <UserControl.DataContext>
        <local:TrangChuViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Background" Value="#37474F"/>
            <Setter Property="Padding" Value="15"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#263238">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="130*"/>
            <RowDefinition Height="250*"/>
            <RowDefinition Height="240*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#37474F" Padding="15,10" Margin="0,0,0,15">
            <Grid>
                <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="Tổng quan hệ thống" Foreground="White" FontWeight="Bold" FontSize="20" Margin="0,0,0,2"/>
                    <TextBlock Text="Xem nhanh thống kê và dữ liệu thống kê" Foreground="#BDC3C7" FontSize="12"/>
                </StackPanel>

                <Button Command="{Binding ExportExcelCommand}" 
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Background="#4CAF50" Foreground="White" BorderThickness="0" 
                        Padding="15,0" Height="32" Cursor="Hand"
                        ToolTip="Xuất toàn bộ dữ liệu Dashboard ra file Excel">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FileExcel" VerticalAlignment="Center" Margin="0,0,8,0" Width="18" Height="18"/>
                        <TextBlock Text="Xuất Báo Cáo Excel" VerticalAlignment="Center" FontWeight="Bold" FontSize="14"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="15,0,15,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                <Grid>
                    <Image Source="/Assets/Icon/staff.png" Stretch="Uniform" Width="32" Height="32" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right">
                        <TextBlock Foreground="White" FontSize="26" FontWeight="Bold" HorizontalAlignment="Right" Text="{Binding SoNhanVien}"/>
                        <TextBlock Foreground="#BDC3C7" FontSize="12" HorizontalAlignment="Right" Text="Nhân viên"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
                <Grid>
                    <Image Source="/Assets/Icon/service.png" Stretch="Uniform" Width="32" Height="32" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right">
                        <TextBlock Foreground="White" FontSize="26" FontWeight="Bold" HorizontalAlignment="Right" Text="{Binding SoKhachHang}"/>
                        <TextBlock Foreground="#BDC3C7" FontSize="12" HorizontalAlignment="Right" Text="Khách hàng"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource StatCardStyle}">
                <Grid>
                    <Image Source="/Assets/Icon/material.png" Stretch="Uniform" Width="32" Height="32" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right">
                        <TextBlock Foreground="White" FontSize="26" FontWeight="Bold" HorizontalAlignment="Right" Text="{Binding SoLoaiVatLieu}"/>
                        <TextBlock Foreground="#BDC3C7" FontSize="12" HorizontalAlignment="Right" Text="Loại vật liệu"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="6" Style="{StaticResource StatCardStyle}">
                <Grid>
                    <Image Source="/Assets/Icon/bill.png" Stretch="Uniform" Width="32" Height="32" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right">
                        <TextBlock Foreground="White" FontSize="26" FontWeight="Bold" HorizontalAlignment="Right" Text="{Binding SoHoaDon}"/>
                        <TextBlock Foreground="#BDC3C7" FontSize="12" HorizontalAlignment="Right" Text="Hóa đơn"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="8" Style="{StaticResource StatCardStyle}">
                <Grid>
                    <Image Source="/Assets/Icon/cashier.png" Stretch="Uniform" Width="32" Height="32" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right">
                        <TextBlock Foreground="#FF8F00" FontSize="24" FontWeight="Bold" HorizontalAlignment="Right" Text="{Binding TongDoanhThuStr}"/>
                        <TextBlock Foreground="#BDC3C7" FontSize="12" HorizontalAlignment="Right" Text="Tổng doanh thu"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Margin="15,0,15,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6.5*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="3.5*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="15"  Background="#37474F" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Grid Grid.Row="0" Margin="10,0,10,10">
                        <TextBlock Text="Thống kê doanh thu" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="Năm:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14" FontWeight="SemiBold"/>
                            <ComboBox ItemsSource="{Binding ListYears}" 
                                      SelectedItem="{Binding SelectedYear}" 
                                      Width="80" 
                                      Background="White"
                                      Foreground="Black" 
                                      BorderBrush="#FF8F00"
                                      Padding="5,2"/>
                        </StackPanel>
                    </Grid>

                    <lvc:CartesianChart Grid.Row="1" Margin="0 10 30 0" Series="{Binding DoanhThu}" Foreground="White">
                        <lvc:CartesianChart.DataTooltip>
                            <lvc:DefaultTooltip Foreground="Black" BulletSize="10" />
                        </lvc:CartesianChart.DataTooltip>
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Tháng" Labels="{Binding Labels}" Foreground="White">
                            <lvc:Axis.Separator>
                                <lvc:Separator Stroke="Transparent" Step="1"/>
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Số tiền" LabelFormatter="{Binding Formatter}" Foreground="White">
                            <lvc:Axis.Separator>
                                <lvc:Separator Stroke="Transparent"/>
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
                </Grid>
            </Border>

            <Border Grid.Column="2" CornerRadius="15" Background="#37474F" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Tỷ lệ loại vật liệu" Foreground="White" FontWeight="Bold" FontSize="16" Margin="10,0,0,10"/>
                    <lvc:PieChart Grid.Row="1" Series="{Binding TyLeLoaiVatLieu}" InnerRadius="30" LegendLocation="Bottom" Foreground="White">
                        <lvc:PieChart.DataTooltip>
                            <lvc:DefaultTooltip Foreground="Black" Background="#F5F5F5" BulletSize="10" />
                        </lvc:PieChart.DataTooltip>
                    </lvc:PieChart>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="3" Margin="15,0,15,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Margin="0,0,15,0" Background="#37474F" CornerRadius="15" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Bảng thống kê công nợ khách hàng" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="-5,0,-5,0">
                        <TabControl.Resources>
                            <Style TargetType="TabItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TabItem">
                                            <Border Name="Border" Background="Transparent" BorderThickness="0,0,0,2" BorderBrush="Transparent" Padding="15,8">
                                                <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Border" Property="BorderBrush" Value="#FF8F00"/>
                                                    <Setter Property="Foreground" Value="#FF8F00"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="False">
                                                    <Setter Property="Foreground" Value="#90A4AE"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TabControl.Resources>
                        <TabItem Header="Tổng hợp">
                            <materialDesign:Card UniformCornerRadius="12" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="5,10,5,0">
                        <ListView Background="#37474F" ItemsSource="{Binding CongNoKhachHang}">
                            <ListView.Resources>
                                <Style TargetType="{x:Type ListViewItem}">
                                    <Setter Property="Foreground" Value="White" />
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="Height" Value="35"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#455A64"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#FF8F00"/>
                                            <Setter Property="Foreground" Value="White" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>

                                <Style TargetType="{x:Type GridViewColumnHeader}">
                                    <Setter Property="Background" Value="#37474F" />
                                    <Setter Property="Foreground" Value="White" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="Padding" Value="10,5" />
                                    <Setter Property="BorderBrush" Value="#455A64" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                                                <Border BorderThickness="0,0,1,1" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListView.Resources>

                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="STT" Width="50">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding STT}" TextAlignment="Center" />
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>

                                    <GridViewColumn Header="Tên khách hàng" Width="250">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding DisplayName}" TextAlignment="Center" />
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>

                                    <GridViewColumn Header="Điện thoại" Width="150">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Phone}" TextAlignment="Center" />
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>

                                    <GridViewColumn Header="Địa chỉ" Width="300">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Address}" TextAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>

                                    <GridViewColumn Header="Tổng nợ (đ)" Width="150">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding CongNoHienTai, StringFormat={}{0:N0}}" TextAlignment="Center" Foreground="#FF8F00" FontWeight="Bold"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                            </materialDesign:Card>
                        </TabItem>
                        <TabItem Header="Nợ quá hạn">
                            <materialDesign:Card UniformCornerRadius="12" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="5,10,5,0">
                                <ListView Background="#37474F" ItemsSource="{Binding DanhSachNoQuaHan}">
                                    <ListView.Resources>
                                        <Style TargetType="{x:Type ListViewItem}">
                                            <Setter Property="Foreground" Value="White" />
                                            <Setter Property="FontSize" Value="13"/>
                                            <Setter Property="Height" Value="35"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#455A64"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter Property="Background" Value="#F44336"/>
                                                    <Setter Property="Foreground" Value="White" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>

                                        <Style TargetType="{x:Type GridViewColumnHeader}">
                                            <Setter Property="Background" Value="#37474F" />
                                            <Setter Property="Foreground" Value="White" />
                                            <Setter Property="FontWeight" Value="Bold" />
                                            <Setter Property="Padding" Value="10,5" />
                                            <Setter Property="BorderBrush" Value="#455A64" />
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                                                        <Border BorderThickness="0,0,1,1" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListView.Resources>

                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Header="Mã Phiếu" Width="100">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding ID}" TextAlignment="Center" />
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>

                                            <GridViewColumn Header="Tên khách hàng" Width="200">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding KhachHang.DisplayName}" TextAlignment="Center" />
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>

                                            <GridViewColumn Header="Ngày xuất" Width="120">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding DateOutput, StringFormat=dd/MM/yyyy}" TextAlignment="Center" />
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>

                                            <GridViewColumn Header="Tiền nợ (>30 ngày)" Width="150">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding SoTienDaThanhToan, StringFormat={}{0:N0}}" TextAlignment="Center" Foreground="#F44336" FontWeight="Bold"/>
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </materialDesign:Card>
                        </TabItem>
                    </TabControl>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="#37474F" CornerRadius="15" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Cảnh báo Tồn kho &amp; Ứ đọng" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    
                    <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
                        <TabControl.Resources>
                            <Style TargetType="TabItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TabItem">
                                            <Border Name="Border" Background="Transparent" BorderThickness="0,0,0,2" BorderBrush="Transparent" Padding="15,8">
                                                <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Border" Property="BorderBrush" Value="#00BCD4"/>
                                                    <Setter Property="Foreground" Value="#00BCD4"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="False">
                                                    <Setter Property="Foreground" Value="#90A4AE"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TabControl.Resources>
                        <TabItem Header="Sắp hết hàng">
                            <materialDesign:Card UniformCornerRadius="12" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0,5,0,0">
                                <ListView Background="#37474F" ItemsSource="{Binding CanhBaoTonKho}">
                                    <ListView.Resources>
                                        <Style TargetType="{x:Type ListViewItem}">
                                            <Setter Property="Foreground" Value="White" />
                                            <Setter Property="FontSize" Value="13"/>
                                            <Setter Property="Height" Value="35"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#455A64"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                        <Style TargetType="{x:Type GridViewColumnHeader}">
                                            <Setter Property="Background" Value="#37474F" />
                                            <Setter Property="Foreground" Value="White" />
                                            <Setter Property="FontWeight" Value="Bold" />
                                            <Setter Property="Padding" Value="10,5" />
                                            <Setter Property="BorderBrush" Value="#455A64" />
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                                                        <Border BorderThickness="0,0,1,1" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListView.Resources>
                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Header="Tên vật liệu" Width="200">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding DisplayName}" TextAlignment="Left" TextTrimming="CharacterEllipsis"/>
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn Header="Tồn kho" Width="100">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding SoLuongTon}" Foreground="#F44336" FontWeight="Bold" TextAlignment="Center"/>
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </materialDesign:Card>
                        </TabItem>
                        <TabItem Header="Hàng ứ đọng">
                            <materialDesign:Card UniformCornerRadius="12" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0,5,0,0">
                                <ListView Background="#37474F" ItemsSource="{Binding HangUDong}">
                                    <ListView.Resources>
                                        <Style TargetType="{x:Type ListViewItem}">
                                            <Setter Property="Foreground" Value="White" />
                                            <Setter Property="FontSize" Value="13"/>
                                            <Setter Property="Height" Value="35"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#455A64"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                        <Style TargetType="{x:Type GridViewColumnHeader}">
                                            <Setter Property="Background" Value="#37474F" />
                                            <Setter Property="Foreground" Value="White" />
                                            <Setter Property="FontWeight" Value="Bold" />
                                            <Setter Property="Padding" Value="10,5" />
                                            <Setter Property="BorderBrush" Value="#455A64" />
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                                                        <Border BorderThickness="0,0,1,1" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListView.Resources>
                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Header="Tên vật liệu" Width="150">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding DisplayName}" TextAlignment="Left" TextTrimming="CharacterEllipsis"/>
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn Header="Tồn kho" Width="70">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding SoLuongTon}" TextAlignment="Center"/>
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn Header="Ngày ứ đọng" Width="100">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding SoNgayUDong}" Foreground="#FF9800" FontWeight="Bold" TextAlignment="Center"/>
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </materialDesign:Card>
                        </TabItem>
                    </TabControl>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>