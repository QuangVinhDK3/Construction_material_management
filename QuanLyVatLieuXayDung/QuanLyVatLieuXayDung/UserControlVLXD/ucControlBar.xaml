<UserControl x:Class="QuanLyVatLieuXayDung.UserControlVLXD.ucControlBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyVatLieuXayDung.UserControlVLXD"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             Name="_ucControlBar"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="MouseLeftButtonDown">
            <i:InvokeCommandAction Command="{Binding MouseMoveCommand}"  CommandParameter="{Binding ElementName=_ucControlBar}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid Background="White">
        <materialDesign:ColorZone Padding="5" Background="#E67E22" Height="40">

            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Margin" Value="5 5 5 5"></Setter>
                            <Setter Property="Width" Value="30"></Setter>
                        </Style>
                    </StackPanel.Resources>
                    <Button Command="{Binding MinCommand}" CommandParameter="{Binding ElementName=_ucControlBar}" ToolTip="Đóng" ToolBar.OverflowMode="AsNeeded" Content="{materialDesign:PackIcon Kind=WindowMinimize}" />
                    <Button Command="{Binding MaxCommand}" CommandParameter="{Binding ElementName=_ucControlBar}" ToolTip="Đóng" ToolBar.OverflowMode="AsNeeded" Content="{materialDesign:PackIcon Kind=WindowMaximize}"  />
                    <Button Command="{Binding CloseWindowCommand}" CommandParameter="{Binding ElementName=_ucControlBar}" ToolTip="Đóng" ToolBar.OverflowMode="AsNeeded" Content="{materialDesign:PackIcon Kind=WindowClose}" Background="red" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <TextBlock Margin="16,0,0,0"
                       VerticalAlignment="Center"
                       Text="{Binding Tag,ElementName=_ucControlBar}" FontSize="15" FontWeight="Bold" Foreground="White"  />
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>
    </Grid>
</UserControl>
